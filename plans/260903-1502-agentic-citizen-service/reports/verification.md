# Verification

## Passing gates

- TypeScript: 84/84 tests pass.
- Python Microsoft Agent Framework: 17/17 tests pass.
- TypeScript typecheck, ESLint and Next.js production build pass.
- Default production-browser suite: 9/9 active tests pass; 4 live-model tests are opt-in.
- Official KS browser flow passes end to end with real local ID-porten/Maskinporten test tokens, explicit Fiks consent, five source snapshots, zero automatic fact confirmations and zero model runs.
- Live Cloudflare browser flows pass for multilingual replies, TXT/shared memory/local handoff, and two-page PDF provenance.
- Typed-question live flow verified all model-backed assertions through message creation, revision increase, proposed facts, human gate, model/framework metadata and board rendering. Its final mobile tab locator used exact text while the Punkt badge was included in the accessible name; changed to substring matching. The isolated UI navigation is covered by the default mobile/locale suite. The costly full live flow was not repeated a third time.
- Documentation/About browser check passes NO/EN persistence, all five diagrams, zoom, Mermaid downloads, mobile 375 px layout and no page errors.
- All exported diagram SVGs are valid XML and all Mermaid source copies match.
- ZIP audit: no `.env.local`, token, runtime checkout, database, virtualenv, node_modules, build cache or synced `sources/`.

## Real model evidence

Qwen coordinator and Gemma specialists run through Python Microsoft Agent Framework and Cloudflare. Earlier focused evaluation records remain in this report directory. Live browser calls can vary in latency and may choose to ask a question rather than propose an optional fact; tests accept either safe outcome while requiring exact source provenance for any proposed fact.

## Product boundaries

The KS services contain synthetic workshop data. The app prepares a local packet and never submits an application or creates a municipal decision. The deterministic SFO result is a KS sandbox snapshot; conversational corrections require human review and do not rewrite it.
